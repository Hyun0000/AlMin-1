<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Comments">
<!-- ================================================= 후기 작성 ================================================= -->





<!-- 
INSERT INTO COMMENTS_AIR_MAP VALUES 
(1, (SELECT AIR_NO FROM COMMENTS_AIR WHERE AIR_NAME ='재밌어요'));
 -->

	<insert id="insertGood" parameterType="java.util.List">
		INSERT INTO COMMENTS_GOOD_MAP VALUES
		<foreach collection="list" item="item" separator="," open="(" close=")" index="i">
		(1, (SELECT GOOD_NO FROM COMMENTS_GOOD WHERE GOOD_NAME = #{item[#{i}]}))
		</foreach>
	</insert>
	<!-- open="(" close=")"  -->
	
<!-- 	<insert id="insertBad">
		INSERT INTO COMMENTS_BAD_MAP VALUES
		<foreach collection="list" item="item" open="(1," close=")">
			#{item}
		</foreach>
	</insert>
	
	<insert id="insertCon">
		INSERT INTO COMMENTS_CONDITION_MAP VALUES
		<foreach collection="list" item="item" open="(1," close=")">
			#{item}
		</foreach>
	</insert>
	
	<insert id="insertAir">
		INSERT INTO COMMENTS_AIR_MAP VALUES
		<foreach collection="list" item="item" open="(1," close=")">
			#{item}
		</foreach>
	</insert>
	
	<insert id="insertPay">
		INSERT INTO COMMENTS_PAY_MAP VALUES
		<foreach collection="list" item="item" open="(1," close=")">
			#{item}
		</foreach>
	</insert> -->













	<!-- ================================================= 후기 키워드 가져오기 ================================================= -->
	 <!-- COMMENTS_AIR -->
	 <select id="selectAllCommentsAir" resultType="string">
	 	SELECT AIR_NAME FROM COMMENTS_AIR ORDER BY AIR_NO
	 </select>
	 
	   <!-- COMMENTS_CONDITION -->
	 <select id="selectAllCommentsCondition" resultType="string">
	 	SELECT CONDITION_NAME FROM COMMENTS_CONDITION ORDER BY CONDITION_NO
	 </select>
	 
	 <!-- COMMENTS_PAY -->
	 <select id="selectAllCommentsPay" resultType="string">
	 	SELECT PAY_NAME FROM COMMENTS_PAY ORDER BY PAY_NO
	 </select>
	 
	 <!-- COMMENTS_GOOD -->
	 <select id="selectAllCommentsGood" resultType="string">
	 	SELECT GOOD_NAME FROM COMMENTS_GOOD ORDER BY GOOD_NO
	 </select>
	 
	 <!-- COMMENTS_BAD -->
	 <select id="selectAllCommentsBad" resultType="string">
	 	SELECT BAD_NAME FROM COMMENTS_BAD ORDER BY BAD_NO
	 </select>
</mapper>